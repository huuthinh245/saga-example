import { connectionReducer as connection } from './connection';
import { authReducer as auth } from './auth';
import { eventReducer as listEvent } from './eventReducer';

export default {
  connection,
  auth,
  listEvent
};
