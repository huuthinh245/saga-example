import { all, takeEvery } from 'redux-saga/effects';

// import types from '../types';
// import counterSagas from './counterSaga';

// export default function* rootSaga() {
//   yield all([
//     takeEvery(types.counterTypes.INCREMENT, counterSagas.increaseCounterSaga),
//     takeEvery(types.counterTypes.DECREMENT, counterSagas.decreaseCounterSaga)
//   ]);
// }
